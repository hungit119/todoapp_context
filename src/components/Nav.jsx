import React from "react";

const Nav = () => {
  const style = {
    backgroundColor: "#ccc",
    color: "#000",
    padding: "6px",
    display: "flex",
    justifyContent: "center",
  };
  return (
    <div style={style}>
      <h2>Danh sách việc cần làm</h2>
    </div>
  );
};

export default Nav;
